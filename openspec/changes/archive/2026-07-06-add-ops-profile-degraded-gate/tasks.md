# Tasks

## 1. Spec

- [x] Add OpenSpec delta for owned degraded ops-profile behavior.

## 2. Gate

- [x] Add Playwright route interception for degraded generic read paths.
- [x] Assert graph error, readiness matrix evidence, and admin/nav survival.
- [x] Keep the scenario product-agnostic and read-only.

## 3. Verification

- [x] Run `npm run lint`, `npm run check`, focused tests, `task
openspec:validate`, and `task test:e2e:ops-profile`.
