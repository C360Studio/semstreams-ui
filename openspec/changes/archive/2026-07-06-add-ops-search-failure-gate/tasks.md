# Tasks

## 1. Spec

- [x] Add OpenSpec delta for an owned local search-failure gate.

## 2. Gate

- [x] Add Playwright GraphQL interception scoped to submitted entity-search requests.
- [x] Assert search error locality and graph/admin/trajectory survival.
- [x] Keep the scenario product-agnostic and read-only.

## 3. Verification

- [x] Run `npm run lint`, `npm run check`, focused tests, `task
  openspec:validate`, and `task test:e2e:ops-profile`.
