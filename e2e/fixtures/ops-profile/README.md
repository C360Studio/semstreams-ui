# Ops Profile Fixture

Small SemStreams-compatible read-side fixture used by
`task test:e2e:ops-profile`.

It is intentionally product-neutral and static. The goal is to prove the UI-owned
ops-console contract without requiring a SemSource, SemDev, or sibling backend
checkout.
